[project]
name = "acquisition-risk-cockpit"
version = "0.1.0"
description = "Databricks App surfacing the federal contract risk review queue"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn>=0.30",
    "databricks-sdk>=0.40",
    "psycopg[binary,pool]>=3.2",
    "pydantic>=2.7",
]
